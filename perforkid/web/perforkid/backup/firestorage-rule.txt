rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write, update, delete: if true;
    }
    match /users/{userId}/{allPaths=**} {
      allow read, write, update, delete: if true;
    }
  }
}